// NOTE: This is an example enum so that the import works when we copy/paste into the "interface" project
// nothing needs to change. If the enum is updated in Interface this enum might need to change.
export enum ChainId {
  MAINNET = 1,
  ROPSTEN = 3,
  RINKEBY = 4,
  GÖRLI = 5,
  KOVAN = 42,
  BSC_MAINNET = 56,
  BSC_TESTNET = 97,
  POLYGON_MAINNET = 137,
  MUMBAI_TESTNET = 80001,
  AMOY_TESTNET = 80002,
  HARMONY_MAINNET = 1666600000,
  HARMONY_TESTNET = 1666700000,
  BUILD_BEAR = 19496,
  BASE = 8453,
  CRONOS = 25
}
